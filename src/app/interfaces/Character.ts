export interface Character {
  _id?: (string);
  name: string;
  role: string;
  description: string;
  age: number;
  personality: string;
  hability: string;
  createdAt?: (Date);
  creatorName: string;
  avatarPath: string;
}